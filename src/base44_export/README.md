This directory is the drop-zone for the Base44 frontend export.

Place the extracted Base44 React app files here, then wire up the imports
from src/lib/supabaseClient.js and src/lib/stripeClient.js as needed.
